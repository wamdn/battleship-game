import Board from "./Board";

class ShipBoard extends Board {
   constructor(rootElm) {
      super(rootElm)
      this.tBody.id = 'shipBoard'
   }
   addShip() {
      
   }
   removeShip() {
      
   }
}

export default ShipBoard